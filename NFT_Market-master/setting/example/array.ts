/*
    const arr = [1,2,3,4,5]
    arr.reduce((acc,number)=>{
        acc += 1
        return acc
    },'')

    const a = [{name:'ingoo'},{name:'web7722'}]
    const newA = a.filter(v=> v.toString())
*/
